import { Typography } from "@mui/material";
import React from "react";

const CheckoutPage = () => {
  return (
    <Typography variant="h3">Only logged in users can see this!</Typography>
  );
};

export default CheckoutPage;
